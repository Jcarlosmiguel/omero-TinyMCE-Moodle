define("tiny_omeroembed/options", ["exports"], function(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {value: true});
    // No plugin-specific options needed - editor_tiny/options' own
    // getContextId() is used directly in ui.js. This module only exists
    // because plugin.js calls Options.register(editor) as standard
    // boilerplate, matching every other tiny plugin.
    exports.register = function() {
        // Nothing to register.
    };
});
